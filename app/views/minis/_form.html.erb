<%= form_for(@mini) do |f| %>
  <% if @mini.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mini.errors.count, "error") %> prohibited this mini from being saved:</h2>

      <ul>
      <% @mini.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :film_title %><br>
    <%= f.text_field :film_title %>
  </div>
  <div class="field">
    <%= f.label :imdb_id %><br>
    <%= f.text_field :imdb_id %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
